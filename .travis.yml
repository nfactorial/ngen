language: cpp

compiler:
 - clang
 - gcc

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y cmake
  - sudo apt-get install -y sdl2-2.0

before_script:
  - mkdir build 
  - cd build
  - cmake ..

script:
  - make
